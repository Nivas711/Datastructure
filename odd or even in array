#include<stdio.h>
int main()
{
	int num[]={1,4,2,31,43};
	int i,size;
	size=sizeof(num)/sizeof(num[0]);
	for(i=0;i<size;i++)
	{
		if(num[i]%2==0)
		{
		printf("the even number is : %d\n",num[i]);
	    }
	    else
	    {
	    	printf("the odd number is : %d\n",num[i]);
		}
	}
}
